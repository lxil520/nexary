package org.nexary.governance.platform;

/** Fixed asset kinds used by the read-only governance platform topology. */
public enum GovernanceAssetKind {
    WORKSPACE,
    ENVIRONMENT,
    TEAM,
    SERVICE,
    CLUSTER,
    ZONE,
    INSTANCE,
    MIDDLEWARE
}
