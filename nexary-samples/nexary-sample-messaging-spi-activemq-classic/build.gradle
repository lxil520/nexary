plugins {
    id 'application'
}

dependencies {
    // SPI/provider dependency adoption path:
    // - after publication, import com.aweimao:nexary-bom:${nexaryVersion}.
    // - compile business code against com.aweimao:nexary-messaging-api only.
    // - add exactly one provider at runtime: com.aweimao:nexary-messaging-activemq-classic.
    // - switching to RocketMQ/Redis/Disruptor means changing the provider module,
    //   not changing facade/controller/consumer code.
    implementation project(':nexary-messaging:nexary-messaging-api')
    runtimeOnly project(':nexary-messaging:nexary-messaging-activemq-classic')

    implementation 'org.springframework.boot:spring-boot-starter'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testRuntimeOnly platform("org.apache.activemq:activemq-bom:${rootProject.ext.activeMqClassicVersion}")
    testRuntimeOnly 'org.apache.activemq:activemq-broker'
}

application {
    mainClass = 'org.nexary.samples.messaging.spi.activemqclassic.app.MessagingSampleApplication'
}
